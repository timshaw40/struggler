# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Test / tooling caches
.pytest_cache/
.hypothesis/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Outputs
logs/
data/
*.pt

# Server-side replay logs (`serve_ui.py --log-dir`): one per game, local state.
replays/

# User-supplied art (scans/PDF-derived images). Never commit these.
ui/assets/
*.pdf

# User-supplied data archives. Never commit these (same rule as the PDFs).
twilight_struggle_sessions.txt

# Local tool caches / vendor downloads (e.g. the VASSAL module).
.cache/
