# CLAUDE.md

This repo's agent instructions live in **[AGENTS.md](AGENTS.md)** — it is the
single source of truth: the `docs/` contract, conventions, layout, and the
`## Agent skills` block (issue tracker + domain docs).

Claude Code: the import below pulls it in.

@AGENTS.md
